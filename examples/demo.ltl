// examples/demo.ltl — runnable demo for ltl_diff

import std::io
use ltl_diff as lib

fn main() {
    io::println("ltl_diff demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}